name: Update Aldi Offers (알디 할인 행사 업데이트)

on:
  # 매주 월요일 새벽 2시 (한국시간 10시)에 자동 실행
  schedule:
    - cron: '0 2 * * 1'
  # 수동으로도 실행 가능
  workflow_dispatch:

jobs:
  update-offers:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install dependencies
        run: npm ci
          
      - name: Install Puppeteer with Chrome
        run: |
          npm install puppeteer
          npx puppeteer browsers install chrome
            
      - name: Run Aldi crawler
        run: node scripts/crawl_aldi.js
          
      - name: Commit and push if changes exist
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add scripts/aldi_offers.json
          git diff --staged --quiet || git commit -m "🤖 Auto-update Aldi offers for $(date +'%Y-%m-%d')"
          git push
